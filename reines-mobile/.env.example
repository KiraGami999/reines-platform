# Backend API base URL (no trailing slash).
# Local dev: use your PC's LAN IP so a physical phone can reach the server.
# Production: https://yourdomain.com
EXPO_PUBLIC_API_URL=http://192.168.1.124:3000
